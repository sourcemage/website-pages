= TRIGGER_CHECK =

High triggers magic.

Contains instructions to check every trigger in the [[Spell/Files/TRIGGERS|TRIGGERS]] file. The default is to just display a triggering message and query if a user wants to execute it.