= TRIGGER_CHECK =

High triggers magic.

Checks every trigger in [[Spell/Files/TRIGGERS|TRIGGERS]] file. The default is to just display a triggering message and query if a user wants to execute it.